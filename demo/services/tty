# tty -- interactive shell on console
# Runs bash with console access (job control enabled).
# Restarts when shell exits.
type = process
command = /bin/bash --login
options = runs-on-console unmask-intr
restart = yes
stop-timeout = 3
term-signal = SIGHUP
depends-on: system-init
