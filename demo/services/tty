# tty -- interactive shell on console
# Runs /bin/sh with console access (job control enabled).
# Restarts when shell exits.
type = process
command = /bin/sh
options = runs-on-console unmask-intr
restart = yes
stop-timeout = 3
term-signal = SIGHUP
depends-on: system-init
